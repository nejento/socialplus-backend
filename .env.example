# Environment variables declared in this file are automatically made available to Prisma.
# See the documentation for more detail: https://pris.ly/d/prisma-schema#accessing-environment-variables-from-the-schema

# Prisma supports the native connection string format for PostgreSQL, MySQL, SQLite, SQL Server, MongoDB and CockroachDB.
# See the documentation for all the connection string options: https://pris.ly/d/connection-strings

NODE_ENV=production

# Nastavení serveru
# Nastavte na 0.0.0.0 pro nabindování na všechna rozhradní (production), 127.0.0.1 pro localhost only
SERVER_HOST=0.0.0.0
SERVER_PORT=8080

# SSL/TLS konfigurace pro HTTP/2 (volitelné)
# Nastavte cesty pro SSL certifikát a soukromý klíč a povolte tak HTTPS s HTTP/2
SSL_CERT_PATH=/path/to/your/certificate.crt
SSL_KEY_PATH=/path/to/your/private.key

# Content Security Policy is automatically configured based on NODE_ENV:
# - In production: Strict CSP with limited sources
# - In development: Relaxed CSP for local development

# HSTS (HTTP Strict Transport Security) Configuration
# Only applies when SSL certificates are configured
HSTS_MAX_AGE=31536000
HSTS_INCLUDE_SUBDOMAINS=true
HSTS_PRELOAD=false

# CORS (Cross-Origin Resource Sharing) Configuration
# By default, API endpoints are accessible from anywhere (open API)
# Set CORS_RESTRICT=true to enable origin restrictions
CORS_RESTRICT=false
# When CORS_RESTRICT=true, specify comma-separated list of allowed origins
CORS_ALLOWED_ORIGINS=https://yourdomain.com,https://app.yourdomain.com

# Údaje pro připojení k MySQL databázi
DATABASE_URL="mysql://user:pass@server:port/db"
SHADOW_DATABASE_URL="mysql://user:pass@server:port/db"

# Údaje pro připojení k InfluxDB databázi
INFLUXDB_URL=http://your-influxdb-server:8086
INFLUXDB_TOKEN=your-influxdb-token
INFLUXDB_ORG=socialplusorg
INFLUXDB_BUCKET=socialplus

# Adresář pro skladování logů v JSON formátu
LOGS_DIR=./logs
# Adresář pro ukládání nahraných souborů
UPLOADS_DIR=./uploads
# Limity velikosti souborů v bajtech
MAX_FILE_SIZE=5000000

# Security Configuration
SALT_ROUNDS=10
SESSION_SECRET=session_secret_of_32_characters_or_more
